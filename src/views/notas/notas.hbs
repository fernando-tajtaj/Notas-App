<div class="row">
    {{#each listadoNotas}}
    <div class="col-sm-3">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{titulo}}</h5>
                <p class="card-text">{{descripcion}}</p>
                <a href="#" class="btn btn-primary">Editar</a>
            </div>
        </div>
    </div>
    {{/each}}
</div>